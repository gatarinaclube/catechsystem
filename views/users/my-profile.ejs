<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <title>Meu Dados - CaTech</title>
  <link rel="stylesheet" href="/css/theme.css" />
  <link rel="stylesheet" href="/css/layout.css" />
</head>
<body>
<div class="shell">
  <%- include('../partials/sidebar', { user, currentPath }) %>

  <main class="main">
    <div class="header">
      <div>
        <div class="title-main">Meus Dados</div>
        <div class="title-sub">
          Visualize e edite seus dados cadastrais
        </div>
      </div>
    </div>

    <form method="post" action="/meus-dados">
      <div class="content">

        <div class="section">
          <div class="section-title">Dados editáveis</div>

          <div class="grid-3">
            <div class="field">
              <label class="field-label">Nome</label>
              <input type="text" name="name" value="<%= user.name || '' %>" />
            </div>

            <div class="field">
              <label class="field-label">CPF</label>
              <input type="text" name="cpf" value="<%= user.cpf || '' %>" />
            </div>

            <div class="field">
              <label class="field-label">País</label>
              <input type="text" name="country" value="<%= user.country || '' %>" />
            </div>
          </div>

          <div class="grid-3">
            <div class="field">
              <label class="field-label">Endereço</label>
              <input type="text" name="address" value="<%= user.address || '' %>" />
            </div>

            <div class="field">
              <label class="field-label">Cidade</label>
              <input type="text" name="city" value="<%= user.city || '' %>" />
            </div>

            <div class="field">
              <label class="field-label">Estado</label>
              <input type="text" name="state" value="<%= user.state || '' %>" />
            </div>
          </div>

          <div class="grid-3">
            <div class="field">
              <label class="field-label">CEP</label>
              <input type="text" name="cep" value="<%= user.cep || '' %>" />
            </div>

            <div class="field">
              <label class="field-label">Telefone(s)</label>
              <input type="text" name="phones" value="<%= user.phones || '' %>" />
            </div>

            <div class="field">
              <label class="field-label">E-mail</label>
              <input type="email" name="email" value="<%= user.email || '' %>" />
            </div>
          </div>
        </div>

        <div class="section">
          <div class="section-title">Dados somente para visualização</div>

          <div class="grid-3">
            <div class="field">
              <label class="field-label">Perfil</label>
              <input type="text" value="<%= user.role %>" readonly />
            </div>

            <div class="field">
              <label class="field-label">Clubes</label>
              <input type="text" value="<%= user.clubs || '-' %>" readonly />
            </div>

            <div class="field">
              <label class="field-label">Gatil FIFe</label>
              <input type="text" value="<%= user.fifeCatteryName || 'Não informado' %>" readonly />
            </div>
          </div>

          <p style="margin-top:12px; font-size:12px; color:var(--text-muted);">
            Para alterar outras informações, entre em contato com o administrador.
          </p>
        </div>

        <div class="footer">
          <button type="submit" class="btn btn-primary">
            Salvar alterações
          </button>
        </div>

      </div>
    </form>
  </main>
</div>
</body>
</html>
